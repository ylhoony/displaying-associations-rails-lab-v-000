<% @songs.each do |song| %>
  <p><a href="<%= song_path(song) %>"><%=song.artist_name%> - <%= song.title %></a></p>
<% end %>


<li>
  <a href="<%= song_path(song) %>">
    <%= song.artist_name %>
    -
    <%= song.title %>
  </a>
</li>